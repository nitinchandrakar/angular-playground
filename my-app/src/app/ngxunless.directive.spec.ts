import { NgxunlessDirective } from './ngxunless.directive';

describe('NgxunlessDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxunlessDirective();
    expect(directive).toBeTruthy();
  });
});
